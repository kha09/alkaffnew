# Project Summary: my-v0-project

## Overview
- **Version**: 0.1.0 (private repository)
- **Tech Stack**: Next.js 15.2.4, React v19, Tailwind CSS v3.4.17
- **Key Dependencies**:
  - `react-hook-form` v7.54.1
  - Headless UI: Radix UI components (~30+ packages)
  - UI Libraries: `lucide-react`, `sonner`, `recharts`
  - Tools: `zod` v3.24.1, `date-fns`

## File Structure
```
├── Core: .gitignore, package.json
├── Configurations
│   ├── TailwindCSS & PostCSS configs
│   └── Next.js config (`next.config.mjs`)
├── Source Code
│   ├── `app/` directory structure:
│   │   └── Dashboard subpages (agents, orders, students)
│   ├── `components/` reusable UI elements
│   └── Custom hooks (`hooks/`)
├── Assets: `public/` directory includes SVGs, images
└── Stylesheets (`globals.css`)
```

## Git Integration
- Remote URL: `https://github.com/kha09/alkaffpro.git`
